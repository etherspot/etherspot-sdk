export enum TransactionStatuses {
  Pending = 'Pending',
  Completed = 'Completed',
  Reverted = 'Reverted',
}

export enum TransactionAssetCategories {
  External = 'external',
  Internal = 'internal',
  Token = 'token',
}
